
<h1>Edit temperature</h1>
 
<%= form_with model: @temperature, local: true do |form| %>
 
  <% if @temperature.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@temperature.errors.count, "error") %> prohibited
        this temperature from being saved:
      </h2>
      <ul>
        <% @temperature.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :date %><br>
    <%= form.date_field :date %>
  </p>
 
  <p>
    <%= form.label :temp %><br>
    <%= form.number_field :temp %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>


<%= link_to 'Back', temperature_path %>
